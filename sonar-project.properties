# SonarQube project properties for Keycloak

# Exclude Java files from analysis as compiled classes are not available at this stage.
# To enable Java analysis, the pipeline would need to compile the code before this SonarQube step.
sonar.exclusions=**/*.java

# The following properties are typically auto-detected by sonar-scanner, but can be set explicitly if needed.
# sonar.sources=.
# sonar.sourceEncoding=UTF-8
